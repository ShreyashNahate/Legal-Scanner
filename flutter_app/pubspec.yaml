name: legal_metrology_scanner_app
description: Prototype Flutter app for the Legal Metrology Compliance Scanner (SIH project)
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.2
  http: ^1.6.0
  file_picker: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true