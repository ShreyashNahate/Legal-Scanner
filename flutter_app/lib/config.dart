// Shared config so every screen points at the same backend.
// UPDATE baseUrl to match your setup:
//   - Android emulator          -> http://10.0.2.2:8000
//   - Physical phone on hotspot  -> http://<your-computer-INET-IP>:8000
//     (use the "inet" address from `ifconfig`, NOT the "broadcast" address)
//   - iOS simulator              -> http://localhost:8000
class AppConfig {
  static const String baseUrl = 'http://10.154.55.8:8000';
}
